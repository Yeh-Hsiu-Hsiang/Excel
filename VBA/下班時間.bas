
Sub 下班時間18_00()
'下班時間 18:00

Dim now_address, now_address_Range As Range

Set now_address = ActiveCell    '設定目前儲存格位置
Set now_address_Range = Selection   '設定目前選取儲存格位置

now_address.Value = "18:00"
now_address_Range = "18:00"

End Sub
Sub 下班時間18_30()
'下班時間 18:30

Dim now_address, now_address_Range As Range

Set now_address = ActiveCell    '設定目前儲存格位置
Set now_address_Range = Selection   '設定目前選取儲存格位置

now_address.Value = "18:30"
now_address_Range = "18:30"

End Sub

Sub 下班時間19_00()

Dim now_address, now_address_Range As Range

Set now_address = ActiveCell    '設定目前儲存格位置
Set now_address_Range = Selection   '設定目前選取儲存格位置

now_address.Value = "19:00"
now_address_Range = "19:00"

End Sub


Sub 下班時間19_30()

Dim now_address, now_address_Range As Range

Set now_address = ActiveCell    '設定目前儲存格位置
Set now_address_Range = Selection   '設定目前選取儲存格位置

now_address.Value = "19:30"
now_address_Range = "19:30"

End Sub

Sub 下班時間20_00()

Dim now_address, now_address_Range As Range

Set now_address = ActiveCell    '設定目前儲存格位置
Set now_address_Range = Selection   '設定目前選取儲存格位置

now_address.Value = "20:00"
now_address_Range = "20:00"

End Sub
